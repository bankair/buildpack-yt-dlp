#!/usr/bin/env bash

set -eo pipefail
BUILD_DIR=${1:-}
echo "Build dir: $BUILD_DIR"
echo "Path dir: $PATH"
cd $BUILD_DIR
cd bin
curl --location https://github.com/yt-dlp/yt-dlp/releases/latest/download/yt-dlp
chmod a+rx yt-dlp
./yt-dlp --version
